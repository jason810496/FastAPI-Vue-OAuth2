apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: oauth2-fullstack
spec:
  selector:
    app: nginx-vue3
    tier: frontend
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 80
  type: LoadBalancer